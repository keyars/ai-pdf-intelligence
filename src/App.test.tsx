import{describe,it,expect}from'vitest';import{render,screen}from'@testing-library/react';import App from'./App';describe('AI PDF Intelligence',()=>{it('renders the document workspace',()=>{render(<App/>);expect(screen.getByText('DOCUMENA')).toBeInTheDocument();expect(screen.getByRole('button',{name:/Ask with context/i})).toBeInTheDocument();expect(screen.getByText('Q3 Product Strategy')).toBeInTheDocument()})});